name: Syntax check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  bash-syntax:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Check shell syntax
        run: |
          for file in bbr-tune.sh scripts/sync-from-upstream.sh tests/smoke.sh tests/bbr-enhancements.sh; do
            bash -n "$file"
          done

      - name: Run ShellCheck
        run: shellcheck --severity=warning -x bbr-tune.sh scripts/sync-from-upstream.sh tests/smoke.sh tests/bbr-enhancements.sh

      - name: Verify upstream extraction
        run: scripts/sync-from-upstream.sh --check

      - name: Run smoke tests
        run: tests/smoke.sh
